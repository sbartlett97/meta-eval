# requirements.txt -- Behavioral Invariance Eval Harness (PRD v3.1)
#
# Runtime quantization (BitsAndBytes) is intentionally NOT included: v3.1 uses
# pre-quantized checkpoints served via vLLM / Ollama.

# --- Core ---------------------------------------------------------------
numpy>=1.24
pyyaml>=6.0
requests>=2.31
tenacity>=8.2            # retry/backoff for API + local judge calls

# --- Local model serving ------------------------------------------------
# These are heavy and platform-specific. On Apple Silicon prefer Ollama; install
# vllm only if you use the direct-vLLM path (harness/vllm_server_manager.py).
vllm>=0.3.0
# ollama>=0.1.0         # uncomment if using the Ollama model loader path
# llama-cpp-python>=0.2.0  # uncomment for direct GGUF loading

# --- Remote judge APIs --------------------------------------------------
anthropic>=0.7.0
openai>=1.0.0

# --- Evaluation / analysis ---------------------------------------------
scikit-learn>=1.3.0
sentence-transformers>=2.2.0
pandas>=2.0
matplotlib>=3.7

# --- Development --------------------------------------------------------
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0
mypy>=1.5.0
